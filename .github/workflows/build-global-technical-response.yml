name: SnapBurst PDF Build Archive

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The SnapBurst global technical response PDF has already been published."
